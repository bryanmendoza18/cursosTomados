//imprimir los numeros pares

for(let contador = 0; contador <= 10; contador++){
    if( contador % 2 !== 0){ //si el residuo de contador es dif de cero... osea si no es un num par
        continue; //ir a la sgte iteración
        

    }
    console.log(contador);
}